<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.easandroid">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.AttendanceDetailsActivity"></activity>
        <activity android:name=".Activities.FaceAttendanceActivity" />
        <activity android:name=".MarkAttendanceActivity" />
        <activity android:name=".EmployeeMainActivity" />
        <activity android:name=".EmployeeLoginActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".ViewEmployeeActivity" />
        <activity android:name=".AddEmployeeActivity" />
        <activity android:name=".AttendanceReportActivity" />
        <activity android:name=".PresentEmployeeActivity" />
        <activity android:name=".LoginMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>